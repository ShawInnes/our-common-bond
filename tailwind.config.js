module.exports = {
    darkMode: 'class', // or 'media' for automatic OS-based dark mode
    content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
    theme: {
        extend: {},
    },
    variants: {
        extend: {},
    },
    plugins: [],
}